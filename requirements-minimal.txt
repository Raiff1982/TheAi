# Minimal Codette runtime (CPU-only, pinned)
numpy<2.0
packaging
safetensors==0.4.1
protobuf==3.20.3
sentencepiece==0.1.99
torch==2.1.0
transformers==4.36.0
accelerate==0.25.0
datasets==2.15.0
huggingface-hub==0.20.3

# Web interface (required for app.py)
gradio>=4.0.0

# Optional but lightweight NLP helpers (can be removed if not needed)
nltk
vaderSentiment
textblob
